{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "node.KHR_skeleton_bone.schema.json",
    "title": "KHR_skeleton_bone glTF Node Extension",
    "type": "object",
    "description": "Additional properties and requirements for a bone in a skeleton, representing a segment of a skeletal structure used for animation and posing of 3D models.",
    "allOf": [ { "$ref": "glTFProperty.schema.json" } ],
    "properties": {
        "length": {
            "type": "number",
            "description": "The length of the bone in meters in the +Y direction. If defined, the value MUST NOT be negative. If not defined, this node does not have a defined length, and a length may be inferred through other means.",
            "minimum": 0.0
        },
        "mass": {
            "type": "number",
            "description": "The mass of the bone in kilograms. If defined, positive values indicate a mass in kilograms, or the value 0.0 indicates a bone is massless. If not defined, the bone does not have a defined mass, and a mass may be inferred through other means.",
            "minimum": 0.0
        },
        "shape": {
            "allOf": [{ "$ref": "glTFid.schema.json" }],
            "description": "The index of the shape referenced by this bone, which may be used for visualization, hit registration, ragdoll physics, or any other use case. If not defined, the bone does not specify a shape, and any shape may be inferred by the application. If defined, the `length` property MUST also be defined."
        }
    },
    "dependentRequired": {
        "shape": ["length"]
    }
}
